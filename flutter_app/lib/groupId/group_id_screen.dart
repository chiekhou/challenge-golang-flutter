import 'package:flutter/cupertino.dart';

class GroupIdScreen extends StatelessWidget{
  //final String routeName = '/groupe/:id';
  final int id;
  GroupIdScreen({required this.id});
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}